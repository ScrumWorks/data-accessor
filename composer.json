{
  "name": "scrumworks/data-accessor",
  "description": "Defines data structure contract",
  "license": "MIT",
  "require": {
    "php": "^8.2",
    "scrumworks/enum-mapping": "^1",
    "symfony/serializer": "^7.0"
  },
  "autoload": {
    "psr-4": {
      "ScrumWorks\\DataAccessor\\": "src/"
    }
  },
  "scripts": {
    "phpstan": "php -d memory_limit=2048M vendor/bin/phpstan.phar analyse --level max --ansi src"
  },
  "minimum-stability": "dev",
  "prefer-stable": true,
  "require-dev": {
    "phpstan/phpstan": "^1.10"
  }
}
